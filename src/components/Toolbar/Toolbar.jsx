import React from 'react';
//import './styles.css';

const Toolbar = () => {
  return (
    <div className="toolbar">
      <button>Import Video</button>
      <button>Export</button>
      <button>Cut</button>
      <button>Add Marker</button>
    </div>
  );
};

export default Toolbar;